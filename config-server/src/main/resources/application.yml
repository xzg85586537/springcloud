server:
  port: 8877
eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:9090/eureka/


spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/xzg85586537/springboot-exaple.git
          username: xzg85586537
          password: xiao136325220
          search-paths: /springcloud-example/config

